// CMT I think you can access this using the type prop func (t: Theme) => val; signature
const gradients = {
	"brand-gradient" : "linear-gradient(90deg,#8ea1e1,#7289da)",
};

export default gradients;
